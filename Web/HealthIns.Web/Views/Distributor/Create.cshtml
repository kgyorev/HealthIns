@model HealthIns.Web.InputModels.Bussines.Distributor.DistributorCreateInputModel
@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers

@{
    ViewData["Title"] = "Create";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="container mt-4">
    <form class="my-4" method="post" enctype="multipart/form-data">
        <h2 class="text-center">New Distributor</h2>
        <div asp-validation-summary="ModelOnly" class="text-danger"></div>
        <div class="form-group">
            <label asp-for="HealthInsUserUserName">User Name</label>
            <input type="text" class="form-control" id="healthInsUserUserName" name="healthInsUserUserName" input asp-for="HealthInsUserUserName">
            <span asp-validation-for="HealthInsUserUserName" class="text-danger"></span>
        </div>
        <div class="form-group">
            <label asp-for="OrganizationId">Organization Id</label>
            <input type="text" class="form-control" id="organizationId" name="organizationId" input asp-for="OrganizationId">
            <span asp-validation-for="OrganizationId" class="text-danger"></span>
        </div>
        <div class="form-group">
            <label asp-for="FullName">Full Name</label>
            <input type="text" class="form-control" id="fullName" name="fullName" input asp-for="FullName">
            <span asp-validation-for="FullName" class="text-danger"></span>
        </div>
        <div class="text-center">
            <button type="button" data-toggle="modal" class="btn btn-primary" data-target="#save">
                Save <i class="fas fa-chevron-circle-right"></i>
            </button>
        </div>
        <div class="modal fade" id="save" tabindex="-1" role="dialog"
             aria-labelledby="deleteUserLabel" aria-hidden="true">
            <div class="modal-dialog" role="document">
                <div class="modal-content">
                    <div class="modal-header">
                        <h5 class="modal-title" id="deleteUserLabel">This action can't be undone</h5>
                        <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                            <span aria-hidden="true">&times;</span>
                        </button>
                    </div>
                    <div class="modal-body">
                        Are you sure to save this distributor?
                    </div>
                    <div class="modal-footer">
                        <button type="button" class="btn btn-secondary" data-dismiss="modal">Cancel</button>
                        <button name="action" value="save" class="btn btn-primary" type="submit">
                            Save
                        </button>
                    </div>
                </div>
            </div>
        </div>
    </form>
</div>




@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
